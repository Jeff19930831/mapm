# Dev Log — 2026-06-05

## MAPM

**Memory Recall 搜索策略修复** — 诊断 + 修复 agentmemory 向量搜索命中率
- 根因：con-dev/checkpoint skill 单一泛查询 `"项目名 status decision credentials blocker"` 向量分散导致搜索空
- 修复：改为 3 次聚焦查询并行（2-3 词/次），新增 query 编写原则
- 改动：`agent-kit/claude/skills/{con-dev,checkpoint}/SKILL.md`
- commit: `1e012ac` → `origin/mapm`

## tradingcenter

**收敛整合 + 前端重建 设计定案** — brainstorm 出 spec + 4 阶段 plan，未执行代码
- con-dev 拉取远端（89 files +7648 -707），装 `tradingcenter-core` 后 `pytest` = 1949 passed, 1 skipped（基线锁定）
- 诊断根因：06-02 三仓分拆 vs 06-03 方案3（live 执行进 monolith）战略分叉从未对齐 → 混乱；前端三层不可用
- 用户拍 7 决策：单一 Python 平台 / core 折回 / 前端从零重建（同栈）/ 后端整合先行 / 专门后端四层重组 / 新建 verdict 监控引擎
- 产出：`docs/superpowers/specs/2026-06-05-tradingcenter-consolidation-frontend-rebuild-design.md` + `docs/superpowers/plans/2026-06-05-consolidation-frontend-rebuild.md`
- 06-02/06-04 标 SUPERSEDED；06-03 方案3 成果保留为 `live/` 层主体

## cloud-memory

**云端 agentmemory 全面瘫痪 → 修复** — 全设备记忆写本地、互不可见的根因找到并修复
- 根因：服务器 iii 引擎自动升级到 0.11.6，与 agentmemory 0.9.26 要求的 iii-sdk 0.11.2 不兼容 → `/agentmemory/*` 全 404；且引擎拆成独立 NSSM 服务 `iii-engine` 却处于 Stopped
- 修复（SSH 上服务器）：iii 引擎降 0.11.2（github release，sha256 校验）+ `npm i -g @agentmemory/agentmemory@0.9.26` + `Start-Service iii-engine` + 禁用冲突计划任务
- Mac 侧：停本地 iii + 禁用 LaunchAgent；重启 Claude 后 MCP 实测写云端成功（`mem_mq0i4fkt` 可 recall）
- 真实管理机制更正：NSSM 服务 `iii-engine` + `agentmemory-worker`（非文档写的计划任务）
- 待办：Windows opencode 照 handoff「🪟 Windows 下一步」切云端；服务器禁 iii 自动更新防复发
- 证据：服务器/Mac 直连+代理 `livez=200`，remember/search 实测通过
- 下一 agent：从 plan Phase 0 Task 0.1（折回 core）起，见代码仓 handoff START HERE

## clash-governance

**v3.12 checkpoint 刷新** — 重新按最新 collab-entry/checkpoint 规范收口 Clash 治理仓
- 完成：刷新 `D:\Workspace\clash-governance` 的 handoff/plan/progress
- 完成：补齐 Obsidian 可见目录 `WinCodex/clash-governance/`
- 证据：冲突扫描无命中；Mihomo config test 成功；live Clash 文件仍含 Claude/Anthropic 专用节点和规则
- 注意：`project_registry.py` / `governance_lint.py` 在本机 Jeff1993 仓缺失，本次按 `.project-status.yaml` 手动解析并记录为工具缺口
