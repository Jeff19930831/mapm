## clash-governance

**DIRECT/TUN fake-ip and CC Switch proxy cleanup** — recorded the network recovery checkpoint in the Clash governance repo.
- 完成: added DNS fake-ip filter governance for DIRECT domains and kept domestic AI/control-plane services on DIRECT.
- 完成: documented CC Switch local proxy vs Clash `HTTP_PROXY` boundary and Gemini `settings.json` BOM recovery.
- 证据: `bigmodel.cn` direct HTTPS returned HTTP 200; Gemini settings parses without BOM; checkpoint verification recorded in `progress.md`.

## MAPM

**接力流程 v3.14 + CodeGraph/CODEMAP 分工** — 重划 checkpoint / con-dev / session-handoff 的职责边界。
- 完成: 更新 `collab-entry.md`、`onboarding.md`、Codex skill 说明和 `.agents/skills` 安装副本。
- 完成: checkpoint 默认本地 durable closeout；push/deploy 需明确授权；CodeGraph 只作本地开发雷达，CODEMAP 仍是可提交交接地图。
- 证据: `quick_validate.py` 通过 checkpoint、con-dev、session-handoff、checkpoint-onboarding-ext、con-dev-onboarding-ext 五个 skill。
